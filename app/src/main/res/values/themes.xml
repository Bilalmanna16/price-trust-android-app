<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Pricetrust" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Pricetrust" parent="Base.Theme.Pricetrust" />
    <style name="Theme.Pricetrust.Splash" parent="Theme.SplashScreen">
        <!-- Background color -->
        <item name="windowSplashScreenBackground">@color/white</item>

        <!-- App icon -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

        <!-- Icon background (optional) -->
        <item name="windowSplashScreenIconBackgroundColor">@color/white</item>

        <!-- Theme after splash -->
        <item name="postSplashScreenTheme">@style/Theme.Pricetrust</item>
    </style>

</resources>